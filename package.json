{
  "name": "twitter-server",
  "version": "1.0.0",
  "description": "server with twitter database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Omolara",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
